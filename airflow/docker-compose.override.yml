version: '3'

# docker-compose.override.yml
# ----------------------------
# Overrides for local development with Astro CLI.
# The Postgres service has been removed — data is now stored in Snowflake.
# The scheduler volume mount gives the container access to your local project files.

services:
  scheduler:
    volumes:
      - /Users/nopassword/Documents/Portfolio/exoplanets:/usr/local/airflow/exoplanets